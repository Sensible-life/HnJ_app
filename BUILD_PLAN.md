# BATH PRO / ROOM PRO — APP 빌드 계획안

작성 기준 문서: `[SRS] BATH PRO 및 ROOM PRO 앱 개발 요구사항 정의서 v2.0.md`
작성일: 2026-09-10

---

## 0. 이런 풀스택(앱+웹) 프로젝트는 보통 이 순서로 계획한다

코드를 짜기 전에 아래 순서로 결정해두면 나중에 뒤엎는 일이 줄어든다. 이 문서의 1~8장은 이 틀을 BATH PRO/ROOM PRO에 그대로 적용한 것이다.

1. **문제/사용자 정의** — 누가, 어떤 상황에서, 어떤 문제를 겪는가 (SRS 1장에 이미 정리됨)
2. **요구사항 확정 (SRS/PRD)** — 기능 요구사항 + 비기능 요구사항(성능/보안/호환성) 구분해서 문서화
3. **정보구조(IA) & 화면 플로우** — 메뉴 depth, 화면 간 이동, 권한별로 보이는 화면이 다른지
4. **데이터 모델링 (ERD)** — 엔티티와 관계를 먼저 그리고 나서 API를 설계해야 나중에 스키마 마이그레이션 지옥을 피함
5. **API 설계** — REST/GraphQL 여부, 엔드포인트 목록, 인증 방식(JWT/세션)
6. **기술 스택 & 아키텍처 결정** — 프론트(모바일/웹), 백엔드, DB, 인프라를 "왜 이걸 쓰는지" 이유와 함께 확정
7. **MVP 범위 확정** — 전체 기능 중 "이것 없이는 출시 못 한다" vs "나중에 추가 가능"을 나눔
8. **로드맵 & 마일스톤** — Phase별로 쪼개고, 각 Phase 끝에 "무엇이 동작해야 하는가"를 명시
9. **리스크 목록** — 기술적으로 불확실한 부분(예: NFC, 알림톡 연동)을 먼저 검증(스파이크)
10. **테스트/배포 전략** — 언제부터 QA하고, 스토어 심사 일정은 언제 맞출지
11. **운영 계획** — 출시 후 모니터링, 장애 대응, 버전 업데이트 주기

이 순서를 지키는 이유는 간단하다: **4번(데이터 모델링)과 7번(MVP 범위)을 건너뛰고 바로 코딩을 시작하면**, 보통 중간에 스키마를 갈아엎거나 안 쓸 기능을 먼저 만드는 낭비가 생긴다.

---

## 1. 사용자 & 권한 정리 (SRS 3.1 기반)

| Role | 주 사용 화면 | 핵심 니즈 |
| --- | --- | --- |
| 현장점검자 | 홈(현장자모드), 퀵체크인, ROOM/BATH PRO | 빠른 입력, 한 손 조작, 오프라인 대응 |
| 호텔 담당자 | 리포트, 승인, 이슈트래커, 통계 | 1-Click 승인, 알림 수신 |
| 본사 관리자 | 전체 대시보드, 일정관리, 사용자관리, 통계 | 전체 현황 파악, 스케줄링 |

## 2. 데이터 모델 초안 (ERD 전 단계)

핵심 엔티티만 먼저 도출 — 상세 컬럼은 Phase 1에서 확정:

- `User` (role: 현장점검자/호텔담당자/본사관리자)
- `Hotel` — `Room` (동/층/객실번호, QR/NFC 태그 ID)
- `InspectionSession` (Room FK, 점검자 FK, 상태: 진행중/완료, 시작~종료 시각)
- `InspectionItem` (Session FK, 항목명, 3-State 값, 사진 목록)
- `Media` (S3 경로, 워터마크 메타, before/after 구분, 마킹 오버레이 데이터)
- `IssueTicket` (긴급/주의 항목에서 생성, 상태: 대기/승인/재점검, 비용/자재/재방문일)
- `Report` (Session FK, PDF URL, 웹 리포트 URL, 발송 로그)
- `Notification` (알림톡/Push 발송 이력, 1-Click 승인 토큰)
- `Schedule` (Hotel FK, 정기 방문 주기, 담당자 배정)

## 3. 기술 스택 결정 사항 (제안 — 확정 필요)

| 영역 | 후보 | 선택 이유 |
| --- | --- | --- |
| 모바일 앱 | React Native (Expo dev client / bare) | NFC(FR-INSP-01), 오프라인 SQLite(FR-INSP-05), 카메라 워터마크를 하나의 코드베이스로 iOS/Android 대응 |
| 백오피스 웹 | Next.js + TypeScript | 대시보드/통계, 알림톡 웹뷰 승인 페이지(FR-REP-02)까지 같은 스택으로 커버 |
| 백엔드 API | NestJS (TypeScript) | RBAC, 파일업로드, 알림 발송 등 모듈화가 쉬움 |
| DB | PostgreSQL + Prisma | 관계형 데이터(호텔-객실-세션-이슈) 구조에 적합 |
| 오프라인 동기화 | SQLite(로컬) + 커스텀 sync 큐 | FR-INSP-05 요구사항 직접 대응 |
| 파일 스토리지 | S3 호환 오브젝트 스토리지 | 사진/PDF 저장, 워터마크는 업로드 시 서버(sharp)로 각인 |
| 알림 | FCM(Push) + 카카오 알림톡 API | FR-REP-02 요구사항 |
| PDF 생성 | Puppeteer 기반 HTML→PDF | FR-REP-01 |
| 인프라 | Docker + GitHub Actions + Railway/Fly.io | 소규모 팀 기준 운영 부담 최소화 |

> 스타트업/1인 개발 규모라면 Supabase(Postgres+Auth+Storage+Realtime)로 백엔드 상당 부분을 대체해 개발 속도를 높이는 것도 고려할 것.

## 4. MVP 범위 (1차 출시 기준선)

**MVP 포함**
- RBAC 로그인/자동로그인 (FR-AUTH-01/02)
- QR/NFC 퀵체크인 (FR-INSP-01)
- ROOM PRO 3-State 점검 + 긴급 시 카메라 강제 (FR-INSP-02/03)
- 오프라인 임시저장/동기화 (FR-INSP-05)
- 자동 워터마크 (FR-MED-01)
- 실시간 웹/PDF 리포트 (FR-REP-01)
- 카카오 알림톡 1-Click 승인 (FR-REP-02)

**MVP 이후 (2차)**
- BATH PRO 연동 팝업 (FR-INSP-04)
- Quick-Draw 마킹 도구 (FR-MED-02)
- Before/After 스플릿 슬라이더 (FR-MED-03)
- 객실 타임라인 피드 (FR-REP-03)
- 일정 관리 자동 생성, 통계 대시보드 (5장 관리/설정)

## 5. 로드맵 (Phase 구성)

| Phase | 목표 | 완료 기준 (Definition of Done) |
| --- | --- | --- |
| P0. 기획/설계 | 요구사항·IA 확정, ERD, API 명세, 와이어프레임 검수 | 팀 전원이 화면/데이터 흐름에 동의 |
| P1. 기반 셋업 | 모노레포, CI/CD, 인증/RBAC, DB 마이그레이션 | 로그인 후 role별 빈 화면 진입 가능 |
| P2. 핵심 점검 플로우 | 퀵체크인 + ROOM PRO 3-State + 오프라인 동기화 | 오프라인에서 점검 저장 → 온라인 복귀 시 서버 반영 확인 |
| P3. 미디어 엔진 | 카메라 워터마크, 사진 첨부 | 촬영 즉시 워터마크 각인된 사진이 서버에 저장 |
| P4. 리포트/알림 | PDF/웹 리포트 생성, 알림톡 1-Click 승인 | 긴급 항목 등록 → 담당자 알림톡 수신 → 웹뷰 승인까지 end-to-end |
| P5. BATH PRO 연동 + 이슈트래커 | BATH PRO 팝업, 이슈 상태 추적 | ROOM PRO에서 화장실 이슈 선택 시 BATH PRO 세션 생성 |
| P6. 관리자 웹 고도화 | 일정관리, 사용자관리, 통계 대시보드 | 본사 관리자가 웹에서 전체 현황/통계 조회 가능 |
| P7. QA/최적화/배포 | 성능(3G 2초 이내), 보안(HTTPS/암호화), 스토어 심사 | 스토어 등록 완료, 비기능요구사항 체크리스트 통과 |

## 6. 리스크 & 선검증(Spike) 대상

- NFC 태깅: iOS/Android 라이브러리 호환성, 태그 규격 결정 필요 → 초기에 프로토타입으로 검증
- 카카오 알림톡 연동: 사업자 심사/템플릿 승인에 시간이 걸릴 수 있음 → P0 단계에서 미리 신청
- 오프라인 동기화 충돌 처리: 같은 객실을 두 점검자가 오프라인에서 동시 작업할 가능성 → 충돌 정책 설계 필요
- 이미지 워터마크 성능: 대용량 사진 다수 업로드 시 백그라운드 처리 안정성 (FR 성능 요구사항: 2초 이내 응답)

## 7. 테스트 전략

- 단위 테스트: 백엔드 비즈니스 로직(권한, 상태 전이) 우선
- E2E: 점검 플로우(체크인→3-State 입력→긴급사진→리포트생성→알림톡 승인) 최소 1개 시나리오 자동화
- 오프라인 시나리오 수동 QA: 비행기 모드 테스트 체크리스트 별도 작성
- 실기기 테스트: iOS 14 / Android 8.0 최소 지원 버전 기기 확보

## 8. 배포/운영

- 스토어 심사 일정은 P6~P7 사이에 미리 계정/정책 준비 (앱스토어 심사 기간 고려)
- 백오피스 웹은 스테이징→프로덕션 분리 배포
- 출시 후: 크래시 모니터링(Sentry 등), 알림톡 발송 실패 로그 모니터링

---

세부 실행 항목은 [`TODO.md`](./TODO.md) 참고.
